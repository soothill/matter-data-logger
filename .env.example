# Copyright (c) 2025 Darren Soothill
# Licensed under the MIT License

# Matter Data Logger Environment Configuration
# Copy this file to .env and replace placeholder values with secure credentials
#
# SECURITY WARNINGS:
# - NEVER commit .env file to version control (already in .gitignore)
# - Generate random tokens using: openssl rand -hex 32
# - Use strong passwords (minimum 16 characters, mix of letters, numbers, symbols)
# - Use different credentials for production vs development
# - For production, use HTTPS URLs and TLS certificates

# InfluxDB Initial Setup (used by docker-compose.yml)
INFLUXDB_ADMIN_USERNAME=admin
INFLUXDB_ADMIN_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD
INFLUXDB_ADMIN_TOKEN=CHANGE_ME_GENERATE_WITH_openssl_rand_hex_32
INFLUXDB_ORG=matter-org
INFLUXDB_BUCKET=matter-power
INFLUXDB_RETENTION=30d

# Grafana Configuration
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD

# Application Configuration
INFLUXDB_URL=http://localhost:8086
LOG_LEVEL=info
MATTER_DISCOVERY_INTERVAL=5m
MATTER_POLL_INTERVAL=30s
